spring:
  application:
    name: fresh-config-server # 应用名称
  cloud:
     config:
        server:
          git:
            uri: https://github.com/navyzhou/fresh-config.git #配置文件所在仓库
            username: qfxsxhfy@126.com # 登录账号
            password: ,. # 登录密码
            default-label: master #配置文件分支
            search-paths: src/main/resources/config  #配置文件所在根目录
